package JZOffer;

public class Main44 {
    /**
     * 将101112,...中的每一位称为数位，记为 n;
     * 将10,11,12,...称为数字，记为 num;
     * 数字10是一个两位数，称此数字的位数为2，记为 digit;
     * 每digit位数的起始数字（即:1,10,100,...)，记为start
     * 可推出各digit下的数位数量count的计算公式：count = 9 * start * digit
     * @param n
     * @return
     */
    public int findNthDigit(int n) {
        int digit = 1;
        long start = 1;
        long count = 9;
        return 0;
    }
}
